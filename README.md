# -ngram-language-model
5-Gram Language Model (Edgar Allan Poe Style)

This project implements an n-gram language model with n=5. The model is trained on a public-domain text by Edgar Allan Poe and generates text in a similar literary style.

Files: ngram.py : Python implementation ngram_output.pdf : Sample inputs and generated outputs
